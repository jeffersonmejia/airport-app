# Infrastructure

Infrastructure implementa emisión y validación JWT, el registro de sesión activa en
memoria y la lectura de credenciales de empleados mediante EF Core. El verificador
MD5 existe únicamente para compatibilidad con el dump académico y deberá sustituirse
antes de un uso real.
