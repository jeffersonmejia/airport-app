# Infrastructure

Infrastructure implementa emisión y validación JWT, el registro de sesión activa en
memoria, la lectura de credenciales de empleados y la persistencia de ASP.NET Core
Identity para Google y MFA mediante EF Core. El verificador MD5 existe únicamente
para compatibilidad con el dump académico y deberá sustituirse antes de un uso real.
