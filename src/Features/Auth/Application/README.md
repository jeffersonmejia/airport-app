# Application

Aquí viven los vertical slices `Login`, `Logout` y `GetCurrentUser`, junto con sus
puertos de salida. `Login` y el mapeo de departamentos a roles ya están implementados;
los otros dos slices permanecen pendientes.
